<!--
AI onboarding file.
Mode: bootstrap
Indexed commit: 0677246f53db19f0b34a7b9262e87201dfa33fd0
Last generated: 2026-06-25T11:04:07Z
Generator: generic high-end AI coding agent
Purpose: Help future AI sessions understand this repository quickly.
Audience: Any high-capability AI coding agent, regardless of vendor or model family.
Human edits are allowed. Future refreshes should preserve valid human edits.
-->

# Project Map

## Confirmed top-level files

| Path | Responsibility | Evidence |
|---|---|---|
| `FXNews.mq5` | Product source: a MetaTrader 5 custom indicator for chart-only multi-symbol FX breakout/impulse scanning. | `#property indicator_chart_window`, `#property indicator_plots 0`, source description |
| `README.md` | Human-facing summary, benefits, and wiki links. | README content |
| `.gitignore` | Excludes MT5 compiled/runtime artifacts and temporary files. | `.gitignore` |
| `AI_INDEX.md` | Primary AI onboarding entrypoint. | generated by this run |
| `AGENTS.md` | Generic vendor-neutral AI agent instructions. | generated by this run |
| `.ai/` | Detailed AI onboarding documentation and manifest. | generated by this run |

## Not detected during bootstrap

The following common files or systems were checked and not found in the inspected repository tree: `package.json`, `pyproject.toml`, `Dockerfile`, `.github/workflows/ci.yml`, `CLAUDE.md`, `.github/copilot-instructions.md`, `AI_INDEX.md`, `AGENTS.md`, and `.ai/MANIFEST.json` on `main` before bootstrap.

## Single-file module map inside `FXNews.mq5`

| Area | Key symbols/functions | Responsibility |
|---|---|---|
| Indicator declaration | `#property strict`, `indicator_chart_window`, `indicator_plots 0` | Defines this as a strict chart-window indicator with no plots. |
| Inputs and constants | `input ...`, `DIR_*`, dashboard constants, history capacities | User configuration and runtime bounds. |
| Runtime state | `SymbolProfile`, `CompositeSignalScore`, `DashboardSignal`, `HistoricalBacktestStats`, global arrays | Holds scan profiles, scoring state, dashboard rows, historical stats, caches. |
| Initialization | `InitializeRuntimeSettings`, `OnInit`, `ValidateInputs`, `ParseSymbols`, `ParseTimeframes`, `AllocateHistoryBuffers` | Sets effective settings, validates inputs, builds profiles, starts timer. |
| Timer dispatch | `OnTimer`, `IsHistoricalMode`, `RunHistoricalOperatingMode`, `ScanAll` | Chooses historical validation/autotune or live scan. |
| Market data | `EnsureSymbolReady`, `FindBrokerSymbolMatch`, `UpdateMarketData`, `UpdateRatesData`, `UpdateMovementData`, `UpdateTickQuality` | Selects/resolves symbols and updates ticks, rates, spreads, ATR, movements, tick quality. |
| Currency flow | `CalculateCurrencyStrength`, `EvaluateCurrencyFlowQuality`, `CalculateBasketAgreement`, `DominantCurrencyFlow` | Computes basket confirmation/conflict and correlated alert group ids. |
| Scoring | `BuildCompositeSignalScore`, `EvaluateExecutionQuality`, `EvaluateBreakoutStructure`, `EvaluateImpulseQuality`, `EvaluateRegimeContext`, `EvaluateCalendarContext` | Produces score, reason summary, caps, tags, and block reasons. |
| State machine | `UpdateSignalState`, `ActivateSignal`, `EndActiveSignal`, `StartCooldown`, `IsConfirmedSignal` | Manages candidate/active/expired/cooldown signal state. |
| Alerts | `SendOptionalAlert` | Optional sound and push notification dispatch. |
| Dashboard | `UpdateDashboard`, `ActivityStatusText`, `DiagnosticsText`, `FormatDashboardSignalText`, `SetDashboardRow`, `CleanupDashboardObjects` | Chart label status/history rendering. |
| Historical validation/autotune | `RunHistoricalOperatingMode`, `RunHistoricalBacktest`, `BuildHistoricalSignalScore`, `BuildValidationReport`, `BuildAutotuneReport`, `PrintHistoricalReportToJournal` | Uses M1 history for validation/autotune and journal reports. |
| Utilities | `Clamp`, `SmoothStep`, parsing/formatting/statistics helpers | Shared math, formatting, time/session, and array utilities. |

## Internal dependency flow

```text
OnInit
  -> InitializeRuntimeSettings
  -> ValidateInputs
  -> ParseSymbols -> ParseTimeframes -> ResetProfile
  -> InitializeCalendarCache
  -> AllocateHistoryBuffers
  -> EnsureSymbolReady
  -> EventSetTimer

OnTimer
  -> RunHistoricalOperatingMode  [validation/autotune]
  -> ScanAll                    [live]

ScanAll
  -> UpdateMarketData
  -> CalculateCurrencyStrength
  -> CalculateScoresAndUpdateState
       -> BuildCompositeSignalScore
            -> EvaluateExecutionQuality
            -> EvaluateBreakoutStructure
            -> EvaluateImpulseQuality
            -> EvaluateCurrencyFlowQuality
            -> EvaluateRegimeContext
            -> EvaluateCalendarContext
       -> UpdateSignalState
  -> UpdateAlertGroups
  -> UpdateDashboard / UpdateActivityStatusLine
```

## External dependencies

| Dependency | Use | Evidence |
|---|---|---|
| MetaTrader 5 indicator runtime | Lifecycle callbacks, chart labels, symbol/tick/rates/calendar APIs. | MQL5 APIs in `FXNews.mq5` |
| MetaEditor | Manual compile path; no repo-local compiler script found. | inferred from MQL5 source/project type |
| MT5 market data/history | Live ticks, rates, `CopyTicks`, `CopyRates`. | `FXNews.mq5` |
| MT5 economic calendar | Optional calendar context when enabled. | `CalendarValueHistory`, `CalendarEventById` |
| MT5 notifications/sound | Optional sound and push alerts. | `PlaySound`, `SendNotification` |

## Generated or do-not-edit zones

| Path/pattern | Rule |
|---|---|
| `*.ex5` | Compiled artifact; ignored; do not commit. |
| `*.log`, `*.out`, `*.tmp`, `*.bak` | Runtime/temp artifacts; ignored; do not commit. |
| `MQL5/Logs/`, `MQL5/Profiles/`, `MQL5/Tester/` | Local MT5 runtime/tester output; ignored; do not commit. |
| `.ai/MANIFEST.json` | Machine-readable onboarding manifest; keep valid JSON. |

## Common navigation shortcuts

- Default inputs: top of `FXNews.mq5`.
- Startup failures: `ValidateInputs`, `OnInit`, `EnsureSymbolReady`.
- Missing/invalid symbols: `FindBrokerSymbolMatch`, `SymbolTimeframeUsedByAnotherProfile`.
- Score behavior: `BuildCompositeSignalScore` and evaluator functions.
- Dashboard layout: dashboard constants, `FormatDashboardSignalText`, `FitDashboardText`.
- Historical results: `RunHistoricalBacktest`, report builders, MT5 Journal output.
