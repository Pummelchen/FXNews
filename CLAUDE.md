# FXNews — working agreement

This repository's agent instructions live in **[`AGENTS.md`](AGENTS.md)**. That file
is the single source of truth: the product and its boundaries, the layout, the build
and gate commands, the version-bump procedure, the scoring conventions, and the traps
that have already cost time here.

`CLAUDE.md` exists only because some tools look for that filename. **Do not add
instructions here** — two instruction files that have to be kept in step is how they
start to disagree. The content that used to be here now lives in `AGENTS.md`.
